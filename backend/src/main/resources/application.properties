# Production Configuration
spring.application.name=appointment-api
server.port=8080

# PostgreSQL Configuration (for production)
spring.datasource.url=jdbc:postgresql://localhost:5432/appointmentdb
spring.datasource.username=appointment_user
spring.datasource.password=appointment_password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Disable H2 Console in production
spring.h2.console.enabled=false

# Production JWT secret (use environment variable)
jwt.secret=nuU2vFniLHbGukwdB6bse78WpR4lu1bj64y0E9xl65kgDUdVF6ZOA+/UEOdf4o4/+q7uuR6vRn+RFkkgglLKBw==
jwt.expiration=86400000

# CORS Configuration
cors.allowed.origins=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003

# Logging
logging.level.root=WARN
logging.level.com.appointment.api=INFO

